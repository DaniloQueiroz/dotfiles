[global]
    font = "Monospace 8"
    allow_markup = yes
    sort = yes
    follow = mouse

[shortcuts]
    close = ctrl+escape
    close_all = ctrl+shift+escape
    history = ctrl+apostrophe
    context = ctrl+shift+apostrophe
